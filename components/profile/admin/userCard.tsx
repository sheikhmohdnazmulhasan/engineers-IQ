import { IUserResponse } from "@/interface/user.response.interface";

const UserCard = ({ data }: { data: IUserResponse }) => {
    return (
        <div>

        </div>
    );
};

export default UserCard;